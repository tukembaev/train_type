export type LoginFormValues = {
    username: string;
    password: string;
}
export type RegisterFormValues = {
    id:string |number
    username: string;
    password: string;
    verifyPassword?:string;
}
