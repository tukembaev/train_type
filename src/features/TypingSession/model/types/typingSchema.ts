export interface textSchema {
    data: any;
    isLoading?: boolean;
    isError?: boolean;
}

export enum typingLanguageSelection {
    RUSSIAN = 'russian',
    ENGLISH = 'english',

}

export interface languageSchema {
    data: any;
    isLoading?: boolean;
    isError?: boolean;
}

